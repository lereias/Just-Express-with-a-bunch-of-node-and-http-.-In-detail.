const helmet = require("helmet");
app.use(
  helmet.contentSecurityPolicy({
    directives: {
      defaultSrc: ["'self'", "fonts.googleapis.com"],
      scriptSrc: ["'self'", "maxcdn.bootstrapcdn.com", "ajax.googleapis.com"],
      styleSrc: ["'self'", "maxcdn.bootstrapcdn.com", "fonts.googleapis.com"],
      imgSrc: ["'self' blob:", "image.tmdb.org", "fonts.googleapis.com"],
      objectSrc: ["'self'"],
    },
  })
);


